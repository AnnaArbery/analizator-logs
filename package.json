{
  "name": "analizator-logs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "server": "nodemon index.js --prefix server",
    "client": "npm run dev --prefix client",
    "start": "concurrently \"(npm run client)\" \"(npm run server )\"",
    "setup": "npm i && npm i --prefix client && npm i --prefix server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}
